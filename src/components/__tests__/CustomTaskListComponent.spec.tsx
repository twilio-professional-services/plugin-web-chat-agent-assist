import React from "react";
import { shallow } from "enzyme";

import CannedResponses from "../CannedResponses/CannedResponsesUI";

// describe("CannedResponsesComponent", () => {
//   it("should render demo component", () => {
//     const props = {
//       isOpen: true,
//       dismissBar: () => undefined,
//     };
//     const wrapper = shallow(<CannedResponses {...props} />);
//     expect(wrapper.render().text()).toMatch(
//       "This is a dismissible demo component"
//     );
//   });
// });
